# HEAT-COND Optimization Platform — dépendances Python
#
# Installation : pip install -r requirements.txt
#
# Note : tkinter est livré avec Python sur Windows et macOS.
# Sur Linux (Ubuntu/Debian), installer en plus :
#     sudo apt install python3-tk
# FreeFEM++ doit être installé séparément (https://freefem.org).

numpy>=1.22
scipy>=1.10
pandas>=1.5
matplotlib>=3.5
tqdm>=4.60
python-dotenv>=1.0
