# HEAT-COND optimization platform (FreeFEM++ + Python) - Python dependencies
#
# Installation:
#     pip install -r requirements.txt
#
# Note: tkinter ships with Python on Windows and macOS.
# On Linux (Ubuntu/Debian): sudo apt install python3-tk
# FreeFEM++ must be installed separately (https://freefem.org) to run the solvers.

numpy>=1.22
scipy>=1.10
pandas>=1.5
matplotlib>=3.5
tqdm>=4.60
python-dotenv>=1.0
nbformat>=5.0
scikit-optimize>=0.9   # optional: Bayesian-optimization study (Part I Study 8, Part II)
