# HEAT-COND — plateforme d'optimisation (FreeFEM++ + Python) — dépendances Python
#
# Installation :
#     pip install -r requirements.txt
#     pip install -e .            # (optionnel) rend le package `heatcond` importable partout
#
# Note : tkinter est livré avec Python sur Windows et macOS.
# Sur Linux (Ubuntu/Debian) : sudo apt install python3-tk
# FreeFEM++ doit être installé séparément (https://freefem.org) pour lancer les solveurs.

numpy>=1.22
scipy>=1.10
pandas>=1.5
matplotlib>=3.5
tqdm>=4.60
python-dotenv>=1.0
nbformat>=5.0
scikit-optimize>=0.9   # optional: Bayesian-optimization study (Part I Study 8, Part II)
